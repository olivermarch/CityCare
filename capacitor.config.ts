import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.oliver.march',
  appName: 'citycare',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
